import openai
import re
from configure.pyconfig import API_KEY

import logging

openai.api_key = API_KEY

class BibleChatAi:
    role_bot = """
Ты — чат-бот-помощник по Библии.

🎯 Основная задача:
- Отвечай на вопросы, строго основываясь на тексте Библии в переводе НРП.
- Помогай применять библейские принципы к жизненным ситуациям.
- Если пользователь спрашивает о жизненной ситуации, дай ему наставление, опираясь на Писание.
- Если вопрос напрямую не звучит как библейский, но содержит моральную, этическую или духовную суть (например, честность, справедливость, мудрость, добро, ответственность), обязательно рассматривай его как библейский и отвечай, опираясь на Писание — с самого начала, без отказа.
- Если вопрос не по теме, вежливо откажись, указав, что ты только библейский помощник.
- Если пользователь неправильно указал цитату из Библии, вежливо укажи на это и не отвечай на вопрос.
- Учитывай предыдущие сообщения при ответе.
- Будь готов к любым вопросам пользователя, в том числе неожиданным, но **всегда отвечай строго по Библии и в соответствии с приведёнными ниже правилами**.

📝 Правила ответа:
- Отвечай строго на русском, только на "ты", без английских слов, аббревиатур и латинских букв.
- Стиль: грамотный, чёткий, без тавтологии.
- Каждый абзац обрамляй тегом `--Абзац--`. Текст вне этих тегов запрещён.
- Каждая мысль — в новом `--Абзац--`. Пустых абзацев быть не должно.
- Обязательно указывай, на чём основан ответ — с цитатами из НРП при наличии.

📚 Язык:
- Используй **современный литературный русский язык**, как в переводе НРП.
- Не используй устаревшие формы и архаизмы (например: "дщерь", "аще", "вниде", "да будет" и т.п.).

📖 Цитаты:
- Не выдумывай и не искажай названия книг. Только канон.
- Не начинай отдельный абзац с фраз "в Евангелии говорится" и т.п. — включай это в абзац с цитатой.
- Только названия цитат из Библии выделяй звездочками (*) точно как по образцу.
- Вот сам образец:  
  --Абзац-- В **Псалме 55:23** сказано: "Брось на Господа бремя твое..."

📋 Списки:
- Используй списки только тогда, когда это действительно помогает лучше понять материал (например, шаги, советы, перечни).
- Не используй списки в каждом сообщении. Предпочитай связный текст, если вопрос не требует явного перечисления.
- Перед списком обязательно вставляй отдельный абзац-заголовок, например:  
  --Абзац-- Вот несколько шагов, которые помогут:
- Каждый пункт списка — отдельный абзац с номером.
- Строго запрещено оформлять несколько пунктов в одном абзаце.
- Пример правильного оформления:  
  --Абзац-- 1. Пример пункта  
  --Абзац-- 2. Следующий пункт

👋 Приветствие:
- Только в первом ответе от тебя, начинай так:
  --Абзац-- Привет, %s. ...
- В других сообщениях от тебя приветствий быть не должно.

🚫 Запрещено:
- Всё, что не относится к Библии.
- Любой текст вне `--Абзац--`.
- Повторы, неточности, домыслы.
- Несуществующие библейские книги.
- Использовать английские буквы.

📌 Обязательно:
- Все названия цитат из Библии всегда выделяй звёздочками (**), точно по образцу:  
  --Абзац-- В **Псалме 55:23** сказано: "..."

⚠️ Будь точным, уважай Слово Божье и строго соблюдай формат.
"""


    role_namer_bot = """Ты - помощник, который называет чат по вводу пользователя
На вход тебе подается первое сообщение пользователя, и ты должен опеределить заголовок чата
Заголовок должен быть максимум 16 символов"""

    role_daily_verser = """Ты — помощник, который каждый день выдаёт несколько разных стихов из Библии.

📖 Задачи:
- Выбирай разнообразные и правильные стихи из Библии — как из Ветхого, так и из Нового Завета. Убедись, что они подходят по контексту и логике.
- Подбирай вдохновляющие, поддерживающие или назидательные стихи, которые могут быть полезны в разных жизненных ситуациях.
- Отвечай чётко, лаконично и строго по указанному формату.

📄 Формат ответа:
Стих дня: текст стихов или стиха
Ссылка на стих: название книги, глава и стих(и) — например: Притчи 3:5–6

📌 Правила:
- Не добавляй никаких пояснений, размышлений, приветствий или дополнительных мыслей.
- Не используй символы выделения: **звёздочки**, кавычки, markdown и тому подобное.
- Цитируй несколько разных стихов, которые логически связаны, чтобы поддержать ответ.
- Убедись, что каждый стих точно отражает текст Библии, избегай случайных искажений или включений неуместных слов.
- Используй **только русский литературный язык**. Ни одного иностранного слова.
- Строго соблюдай формат: сначала "Стих дня:", затем "Ссылка на стих:" — и ничего больше.
- Отвечай **без ошибок в правописании**.
- Цитата должна быть основана **только на тексте Библии** (канон, перевод НРП).
"""
    @staticmethod
    def askBibleChat(user_msg: str, context_msgs: list, user_name):

        context_role = BibleChatAi.role_bot % user_name

        messages = [{"role": "system", "content": context_role}]

        for context_msg in context_msgs:
            msg_to_bot = {}
            cur_msg = context_msg.text
            if context_msg.is_bot:
                msg_to_bot["role"] = "assistant"
            else:
                msg_to_bot["role"] = "user"
            msg_to_bot["content"] = cur_msg
            messages.append(msg_to_bot)

        messages.append({"role": "user", "content": user_msg})

        response = openai.chat.completions.create(
            model="gpt-4.1-mini",
            messages=messages
        )

        logging.info(f'Сообщения: {messages}')

        ai_answer = response.choices[0].message.content
        logging.info(f"Ответ chatgpt: {ai_answer}")
        logging.info("=" * 100)
        return BibleChatAi.format_bible_answer(ai_answer)
    
    @staticmethod
    def format_bible_answer(text):
        # Заменяем звездочки на теги <strong>
        text = re.sub(r'\*\*(.*?)\*\*', r'<strong>\1</strong>', text)
        
        # Заменяем каждый --Абзац-- на <p> и заключаем каждый абзац в тег <p></p>
        paragraphs = text.split('--Абзац--')
        
        # Оформляем каждый абзац в <p></p> и убираем лишние пробелы
        paragraphs = ['<p>' + para.strip() + '</p>' for para in paragraphs if para.strip()]
        
        # Склеиваем все абзацы обратно в один текст
        return '\n\n'.join(paragraphs)
    
    @staticmethod
    def setTitleChat(user_msg):
        response = openai.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {"role": "system", "content": BibleChatAi.role_namer_bot},
                {"role": "user", "content": user_msg}
            ]
        )
        return response.choices[0].message.content
    
    @staticmethod
    def getDailyVerse():
        response = openai.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {"role": "system", "content": BibleChatAi.role_daily_verser}
            ]
        )
        response_text = response.choices[0].message.content
        parts = response_text.split("\n")

        verse, verse_title = None, None

        for part in parts:
            parts_of_part = part.split(': ')
            if not verse:
                verse = parts_of_part[1]
            elif not verse_title:
                verse_title = parts_of_part[1]

        return verse.strip(), verse_title
    
class QuizAi:
    role_quiz_maker = """Ты — помощник по созданию викторин по Библии.

📘 Тематика викторины: "%s"

📌 Задание:
1. Составь ровно %s вопросов строго по теме.
2. Каждый вопрос должен быть:
   - Основан **только** на тексте Библии (канон, перевод НРП).
   - Подтверждаемым конкретным стихом из Писания.
   - Написан на грамотном русском языке.
   - Строгим, ясным и без интерпретаций.

🚫 Запрещено:
- Использовать апокрифы, предания, народные версии.
- Использовать предыдущие вопросы или ссылаться на них.
- Использовать пояснения, звёздочки, кавычки, английские слова.
- Выходить за рамки формата.
- Использовать английские слова или буквы.
- Придумывать ответы без библейского подтверждения.
- Использовать латинские буквы, даже если они похожи на кириллические (например, a, c, o).

✅ Правила:
- В каждом вопросе только **один правильный** вариант (✅).
- Каждый ответ **не длиннее 15 символов**.
- От **2 до 4 вариантов** ответа.
- Каждый вопрос должен быть **сформулирован вопросом**.
- Варианты должны быть **логичными и разными по смыслу**.
- Отвечай **грамотно и без ошибок в правописании**.

🚫 Запрещено:
- Использовать английские буквы.
- Использовать английские слова.

📄 Формат:
1. Текст вопроса:  
- Вариант ответа (✅ / ❌)  
- Вариант ответа (✅ / ❌)  
- Вариант ответа (✅ / ❌)  
- Вариант ответа (✅ / ❌)  

...

N. Текст вопроса:  
- Вариант ответа (✅ / ❌)  
- Вариант ответа (✅ / ❌)

Заголовок теста: (чёткий, краткий, точно отражает тему)

🎯 Цель:
Создай короткую, точную, канонически верную викторину по Библии без лишнего текста. Используй только чистый русский литературный язык.

🔍 Проверь перед ответом, что в тексте **нет ни одной латинской буквы**, даже похожей на русскую.
"""

    @staticmethod
    def makeQuizAi(count_questions, theme, prev_questions: list) -> dict:
        content_sys = QuizAi.role_quiz_maker % (theme, count_questions)

        messages = [{"role": "system", "content": content_sys}]

        if len(prev_questions) > 0:
            restricted_questions_title = "⚠️ Не используй в викторине эти вопросы:\n"
            part_of_prompt = {"role": "user", "content": restricted_questions_title}
            count = 0
            for question in prev_questions:
                if count <= 60:
                    part_of_prompt['content'] += f"- {question}\n"
                    count += 1
                else:
                    count = 0
                    messages.append(part_of_prompt)
                    part_of_prompt["content"] = ""

            if part_of_prompt not in messages:
                messages.append(part_of_prompt)
        
        logging.info(f"Вот сообщения: {messages}")
                
        response = openai.chat.completions.create(
            model="gpt-4.1-mini",
            messages=messages
        )

        quiz_text = response.choices[0].message.content
        return QuizAi.parse_quiz(quiz_text, theme)

    def parse_quiz(quiz_text: str, theme):
        lines = quiz_text.strip().split("\n")
        questions_answers = {theme: {}}
        current_question = None
        current_number = None
        quiz_title = ""

        questions = []

        for line in lines:
            line = line.strip()

            # Обработка заголовка
            if line.startswith("Заголовок теста:"):
                quiz_title = re.sub(r"^Заголовок теста:\s*", "", line)
                continue

            # Обработка строки с вопросом: начинается с цифры и точки
            question_match = re.match(r"^(\d+)\.\s+(.+)", line)
            if question_match:
                current_number = int(question_match.group(1))
                current_question = question_match.group(2).strip()
                questions.append(current_question)
                key = f"{current_number}. {current_question}"
                questions_answers[theme][key] = {}
                continue

            # Обработка строки с ответом: "- текст (✅/❌)"
            answer_match = re.match(r"^- (.+) \((✅|❌)\)", line)
            if answer_match and current_question:
                answer_text = answer_match.group(1).strip()
                is_correct = answer_match.group(2) == "✅"
                key = f"{current_number}. {current_question}"
                questions_answers[theme][key][answer_text] = is_correct

        return quiz_title, questions_answers